@using Microsoft.AspNetCore.Identity;
@inject SignInManager<User> SignInManager
@model List<Book>
@{
    ViewBag.Title = "Profile";
    var user = ViewBag.User as User;
    var wishlistBookIds = ViewBag.WishlistBookIds as List<int> ?? new List<int>();
    var cartBookIds = ViewBag.CartBookIds as List<int> ?? new List<int>();
}
<!-- Profile Section -->
<div class="container profile-section">
    @if(user.ProfilePicture != null){
        <img src="data:image/*;base64,@(Convert.ToBase64String(user.ProfilePicture))" alt="Profile Image" class="profile-img">
    } 
    else {
        <img src="~/images/default_user.jpg" alt="Profile Image" class="profile-img">
    }
    <h2 class="profile-name">@User.Identity.Name</h2>
    <h4 class="profile-books">@Model.Count() Books</h4>
</div>
<!-- Books Section -->
<div class="container books-section">
    <h3>My Books</h3>
    <div class="cards">
        @await Html.PartialAsync("_LoadBooksPartial", new ShowBooksViewModel
       {
           books = Model,
           UserCart = cartBookIds,
           UserWishlist = wishlistBookIds
       })
    </div>
</div>

@section Styles {
    <link href="~/css/profile.css" rel="stylesheet" />
}
